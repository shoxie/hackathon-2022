export type PlaceProps = {
    code: string;
    content: string;
    background: string;
    rating: number;
    ratingCount: number;
    toCome: number;
    destinationType?: string;
  };

export type CouponProps = {
    code: string;
    content: string;
    background: string;
}